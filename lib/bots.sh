#!/usr/bin/env bash
#
# The list of bots the harness knows about, defined once.
#
# Sourced by capture.sh, verify-all.sh and sync-lib.sh, which each used to
# carry their own copy -- three places to edit when a bot is added, and three
# chances for them to disagree.

ALL_BOTS="bot-vacation botgov BOUS covid-wastewater dreambot featuring-super-cat finds-you in-this-economy moonstriker rejected-plates"
